---
layout: layout.njk
---

<div class="content-container">

<div class="back-nav">
  <a href="javascript:history.back()" class="back-button">← Back</a>
</div>

<article class="post-full">
  {% if image %}
  <img src="{{ image | url }}" alt="Screenshot of {{ title }}" class="post-main-image">
  {% endif %}
  {# Attempting built-in Nunjucks date filter again #}
  {# Using custom date filters #}
      <p class="post-meta">
        Published on: <time datetime="{{ date | htmlDateString }}">{{ date | readableDate }}</time>
      </p>
  <hr>

  {{ content | safe }}

</article>

</div>